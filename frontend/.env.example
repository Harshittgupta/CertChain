# Leave everything unset for the local anvil demo (defaults are the deterministic
# fresh-anvil addresses). Fill these after deploying to Sepolia.
VITE_ISSUER_REGISTRY=
VITE_CREDENTIAL_REGISTRY=
VITE_CERTIFICATE=
VITE_SEPOLIA_RPC_URL=
# Block number of your deployment, keeps event scans fast on public RPCs.
VITE_DEPLOY_BLOCK=0
